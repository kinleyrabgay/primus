/**
 * Virtualscroller Component Theme
 */
export const virtualscroller = {
    loader: {
        mask: {
            background: '{content.background}',
            color: '{text.muted.color}'
        },
        icon: {
            size: '2rem'
        }
    }
};
